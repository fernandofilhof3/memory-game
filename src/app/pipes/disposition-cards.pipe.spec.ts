import { DispositionCardsPipe } from './disposition-cards.pipe';

describe('DispositionCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new DispositionCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
